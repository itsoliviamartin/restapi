# REST API Go Lang

This an example REST API built with Go lang that represents a list of ablums that lists all albums with a GET request to the endpoint /albums, create a album and add to the list with a POST request to /albums, and you can get the ablums by id as a path parameter to /albums/:id.
